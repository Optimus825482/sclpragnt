# 2026-09-12 — Kapsamlı Uygulama Denetimi (TESPİT aşaması)

Kullanıcı tüm uygulamanın akış sırasına göre denetimini istedi. **Hiçbir kaynak dosya
değiştirilmedi** (git temiz) — yalnızca tespit + araştırma + rapor.

## Yöntem
9 paralel denetim ajanı (birbirini kesmeyen kapsamlar A–I) + 1 bağımsız red-team doğrulama
ajanı (J). Her bulgu `dosya:satır` + somut mekanizma zorunluluğu. Ajanlar 50+ yanlış pozitifi
kendileri eledi.

## Sonuç
**195 bulgu: 10 KRİTİK · 44 YÜKSEK · 75 ORTA · 66 DÜŞÜK.**
Red-team: 12 iddia → 10 doğrulandı, 2 kısmen, **0 reddedildi**.
Çıktılar: `outputs/denetim_2026-09-12/` (ANA_RAPOR.md + A…J + scratch/)

## Durum tespiti (şaşırtıcı olan)
- Testler **381/381 yeşil**, `tsc --noEmit` **0 hata** — buna rağmen 10 kritik hata var.
- Sebep 1: **para matematiğinin hiç testi yok** (PnL, komisyon, boyutlandırma, çıkış).
- Sebep 2: `tests/test_loop_wiring.py:55` koruma testi **boşa çalışıyor** — `re.search(name,
  main_src)` `main.py:81-87`'deki **import bloğuyla** eşleşiyor. 29 `_start_background`
  satırından herhangi biri silinse bile geçiyor. Gerçek kapsam 1/29.

## En yüksek etkili 5 bulgu (doğrulanmış)
1. **ML-01** `ml_forecast.py:336-338` — model **5m** barlarda eğitiliyor, çıkarım **1m** barlarla.
   `ret3_pct` 15dk vs 3dk, `rsi14` 70dk vs 14dk. Ölçülen oranlar 0.36/0.43/2.35.
2. **D-01** `analyzer.py:412-421` — kâr kilidi yalnızca **giriş** komisyon bacağını ekliyor.
   "Kilitli kâr" çıkışı zarar yazıyor: 10.000 TRY'de **−14,02 TRY**.
3. **F-02** `database.py:874-899` — MACD kanıt tablosu hedef ana **ulaşıldı** kontrolü yapmıyor;
   üç ufka (5m/15m/30m) **aynı ~4.5 dk getiri** yazıp satırı mühürlüyor. Doldurucu 120 sn'de
   bir çalıştığı için bu **varsayılan yol**. Kanıt katmanı şu an çöp üretiyor.
4. **V-01** `database.py:419-433` vs `:353-417` — `preview_portfolio_reconcile` ile
   `reconcile_portfolio` farklı formül; iki adımlı onayda admin **yanlış bakiyeyle** onaylıyor.
5. **G-01** `main.py:514` — `truncated` hiç tanımlanmamış (AST ile doğrulandı) → admin export
   her çağrıda `NameError`. Ayrıca LIMIT'siz `SELECT *`.

## Revize edilen şiddetler (red-team sayesinde — not düşüldü)
- `atr_pct` kesir/yüzde uyuşmazlığı (I-01/C-05): **doğru ama tüketicisi yok** → ölü kodda doğru,
  YÜKSEK→DÜŞÜK.
- ATR'nin Wilder olmaması (C-03): **belgelenmiş tercih**, "ATR=0 kırar" alt savı çürütüldü → ORTA.
- H-01 "backend NET": **reddedildi** — bu D-01 ile aynı kök hata, ayrı frontend hatası değil.

## Operasyonel gözlem
`pytest` çalıştırması **özet satırı yazdırmıyor**; çıkışta `couldn't stop thread 'pool-1-worker-N'`
uyarıları geliyor → kapatılmayan bir `ThreadPoolExecutor` var (muhtemelen embedding_worker veya
DB havuzu). Atexit sorunu, bulgu listesine eklenebilir.

## PARTİ 1 — UYGULANDI VE DOĞRULANDI (aynı gün)

Değişen dosyalar: `backend/app/main.py` (+78), `backend/tests/test_security_behavior.py` (+26).
Derleme temiz, test **382/382 yeşil**.

| ID | Değişiklik |
|---|---|
| G-01 | `SELECT *` → `LIMIT _ADMIN_EXPORT_MAX_ROWS+1` (ölü sabit canlandı); `truncated` tanımlandı |
| G-06 | `bootstrap_symbol_activity()` try/except — Binance kesintisi startup'ı öldürüyordu |
| G-08 | `except Exception: pass` → log + `auto_paper_error` alanı |
| G-07 | Ayar kaydı `payload`'ı artık birleştiriyor (restart'ta kaybolma düzeldi) |
| G-03/04 | 11 endpoint'e `_require_admin` (koruma 23 → 34) |

### Öğrenilen dersler (kalıcı değer)
1. **G-07 ↔ G-09 etkileşimi:** ayar kalıcılığını "tüm config'i yaz" diye düzeltmek tehlikeliydi —
   G-09'da bir GET 60 sn'de bir `config.SYMBOLS`'u mutasyona uğratıyor, kalıcı hale gelirdi.
   Çözüm: yalnızca payload'daki anahtarları yaz.
2. **Koruma eklerken testleri hemen çalıştır:** `update_config`'ı `request=None` ile çağıran 2 test
   `AttributeError` verdi. Testler hata sözleşmesini sınıyor, yetkiyi değil → kapı patch'lendi.
   + yeni test: `test_config_update_requires_admin` (403 + `_apply_config_update` çağrılmıyor).
3. **D-01'in ikinci yüzü:** `main.py:1727` AUTO_PAPER bloğu da yalnızca giriş komisyonunu düşüyor.
   `BİLİNEN SORUN` işareti kondu, Parti 3'e bırakıldı (paylaşılan para matematiği).
4. **Çoklu edit tekniği:** aynı dosyada 11 değişikliği tek atomik Python script'i ile uyguladım
   (her `(old,new)` için tam 1 eşleşme zorunlu, aksi halde hiç yazmıyor). Paralel-edit yarışını
   tamamen önler. Script: `outputs/denetim_2026-09-12/scratch/apply_admin_gates.py`.

## Sıradaki adım
PARTİ 2 (ölçüm bütünlüğü: F-02 kanıt tablosu, F-01 hayalet alarm, TAH-02 eventual_hit) —
kanıt katmanı düzeltilmeden eşik/ağırlık ayarlanmamalı. Ardından Parti 3 (para matematiği,
replay şart).

## W3 — ML & velocity eğitim-çıkarım uyumu (2026-09-12)
- **ML-01**: model 5m barla eğ, çıkarım 1m'leydi → aynı özellik adı farklı anlam.
  Eklendi `TRAINING_BAR_MINUTES=5` + `inference_bar_minutes()`. velocity.scan_one
  ve chart_forecast artık **kapanmış 5m barlardan** ML özellik üretir; 5m warmup
  yoksa ML tahmini atlanır. `llm_chat` ghost yolu için 5m teyit bırakıldı.
- **ML-02**: `prepare_journal_samples` karar zaman damgası döndürüyor; `train()`
  holdout'ta yalnız `journal_ts <= times[split-1]` eğitime (kronolojik bütünlük).
- **ML-09**: çekirdek özellik yoksa satır atılır; `atr_pct=None` → NaN (0.0 değil).
- **I-05**: `load_model` feature_version/names/training_bar birebir doğrular.
- **I-01**: `atr_pct` kesir; market_intelligence rejim eşikleri 0.5/0.15 → 0.005/0.0015.
- **I-02**: `_velocity_bollinger_width` kanonik `_bollinger`'e (ddof=0) devir et.)
- **D-04**: tarama son forming mumu düşürür.
- Test: `tests/test_w3_ml_parity_fixes.py` (7 kilit). `_bollinger` ddof=0 → BB genişliği
  ~%2.5 daralabilir; `VELOCITY_MIN_BB_WIDTH_PCT` OOS replay ile yeniden kalibre önerilir.

## W3 üretim regresyonu (I-05 geriye uyumluluk)
Üretim logu: `[ML] artifact uyumsuz (I-05) … training=None` → `/api/chart/*/forecast` 503.
Sebep: I-05 katı doğrulaması `training_bar_minutes` alanı **olmayan** mevcut artifact'ı
(`upside_v3.joblib`) reddetti. Düzeltme: alan **varsa** doğrulanır, yoksa legacy kabul
edilir (`training_bars is None or int(training_bars) == TRAINING_BAR_MINUTES`).
Regresyon kilidi: `test_load_model_accepts_legacy_artifact_without_training_bar`.
**Ders:** şema doğrulaması eklerken mevcut artifact'ları reddetme — geriye uyumluluk şart.

## W4 — Gözlem & kanıt bütünlüğü (F-02, F-01, TAH-02)
- **F-02** (KRİTİK): MACD kanıt tablosu 5m/15m/30m'ye **aynı ~4,5 dk getiriyi** yazıp
  satırı mühürlüyordu. Yeni saf yardımcı `_macd_forward_outcomes(rows, base, t0_ms, now_ms)`
  (`database.py:824`): ufuk ancak hedefi **kapsayan kapanmış** 5m bar varsa mühürlenir
  (`stamp+_MACD_BAR_MS >= target` **ve** `<= now_ms`); yoksa `NULL` kalır, sonraki turda dolar.
- **F-01**: evren-içi min-max → tek sembol evrenden çıkınca diğerleri zıplayıp **hayalet
  sıçrama alarmı**. Düzeltme iki katman: `_stable_range` (p5–p95, `_STABLE_RANGE_PCTL=5.0`;
  `n<20` → min/max) + `_own_activity_changed` kapısı. Alarm `stable_score`'dan; ekrandaki
  `strength` evren min-max **kaldı** (gösterge tanımı değişmedi).
- **TAH-02**: tolerans ufka göre sınırlandı — `effective_hit_grace_minutes = min(grace, horizon)`
  → 5m tahmin artık 19. dk'da "isabet" sayılamaz. Tek kaynak `forecast_learning.py`;
  `database.py:2106/2349` + `llm_chat.py:362` buna bağlandı. Belirlenimci (tarama
  gecikmesinden bağımsız).
- Test: `tests/test_w4_evidence_integrity.py` (14). Rapor: `FIX_W4_gorev_kanit_butunlugu.md`.

## W5 — Frontend net esas + null renk sözleşmesi (H-01/H-02/H-03)
- **Tek kaynak** `frontend/app/lib/pnl.ts`: `netOpenPnlTry` = brüt − `commission_pct × q × (e+c)`
  (gidiş-dönüş, D-01 ile aynı esas). `null` → `null` (0 DEĞİL). Tüketiciler: portfolio,
  ana sayfa, charts.
- Komisyon yayını: WS `portfolio` (`runtime.py:211`) + `GET /api/config` (`main.py:1122`) →
  `applyCommissionPct` (geçersiz değeri yok sayar).
- **Devam partisi (kalan tutarsızlıklar):** portföy ana pozisyon **satır** hücreleri hâlâ
  giriş-bacağı `p.pnl_try` gösteriyordu → kanonik `netOpenPnlTry`'a çevrildi (toplam ile
  aynı esas). Özet kartlarında `?? 0` → `null`/"—" (portföy yüklenmeden yeşil ₺0,00 yok).
  `history/page.tsx` özet kartları `t.pnl ?? 0` yerine yalnız ölçülebilir PnL toplar.
  Hedef/`expected_price`/`win_rate`/`in_sample_accuracy`/`confidence` hücrelerindeki
  sabit yeşil + `|| 0` temizlendi.
- Rapor: `FIX_W5_frontend_net_notr.md`. `tsc --noEmit` 0 hata.
- **Bilinçli olarak DEĞİŞTİRİLMEDİ:** backend `pnl_try` hâlâ yalnız giriş bacağı
  (`main.py:1677-1680`, `runtime.py:152-155`) — muhasebe/reconciliation doğru kalsın diye.

## W6 — Kablolama koruması + para matematiği testleri (G-02, I-08)
- **G-02**: `test_loop_wiring.py` boşa çalışıyordu — `re.search(name, main_src)` import
  bloğuyla eşleşiyordu (kapsam 1/29). Yeniden yazıldı: `_START_BG_RE` / `_CREATE_TASK_RE`
  ile **gerçek başlatma** aranır; import/isim geçişi yetmez; girintili tanımlar görülür.
  **Mutasyon kanıtı**: `_start_background(ml_training_loop,…)` silinince test KIRILIYOR.
- **I-08**: `tests/test_w6_money_math.py` (24, 5 sınıf). `min_net_exit_pct`'in `0` davranışı
  (varsayılan emir proxy'si) **kilitlendi**, sessizce değiştirilmedi.
- Test sayısı: 381 → 416 → 442 → **465** (son ölçüm: 465 nokta, 0 F/E, exit 0).
- Rapor: `FIX_W6_kablolama_para_testleri.md`.

## Durum
W1–W6 tamamlandı. Tüm denetim bulgularının düzeltmesi uygulandı ve kilitlendi.
Kalan tek açık iş: backend **muhasebe** tarafını görüntü ile eşitlemek
(`min_net_exit_pct`'i yayınla + reconciliation'ı yeniden doğrula) — ayrı iş.

## W5 kapanış partisi — yarış hatası ve doğrulama (2026-09-12, akşam)

**Yapılan (W5 residual):** portföy ana pozisyon **satır** hücreleri kanonik
`netOpenPnlTry`/`netOpenPnlPct`'e çevrildi (toplamla aynı esas). Portföy özet
kartlarında `?? 0` → `null`/"—" (`totalValue`, `freeTry`, `realizedTotal`); WS fallback
snapshot'ı eksik alanları `0`'a çevirmiyor (`undefined`); `Portfolio` tipinde
`try`/`total_value` opsiyonel. `history/page.tsx` özet kartları yalnız **ölçülebilir**
PnL toplar (dosyanın kendi tip notu bunu zaten yasaklıyordu). Hedef/`expected_price`/
`win_rate`/`in_sample_accuracy`/`confidence` hücrelerindeki sabit yeşil + `|| 0` temizlendi
(charts, reports, monitoring, forecasts).

**HATA — aynı dosyaya PARALEL Edit yarışı:** Bir mesajda aynı dosyaya 4 Edit birden
gönderdim (charts ×4, reports ×4, portfolio ×3, monitoring ×2). Edit'ler read-modify-write
yaptığı için **son yazan öncekini ezdi** → 10 düzenleme sessizce kayboldu. Belirti:
`grep` eski metni göstermeye devam etti. **Bu, hafızada Parti 1'de zaten yazılı olan
dersin tekrarıydı.**
**Çözüm:** `outputs/denetim_2026-09-12/scratch/apply_w5_residual.py` — her `(old,new)`
için dosyada **tam 1 eşleşme** zorunlu, hepsi doğrulanmadan **hiçbir dosya yazılmaz**.
8 düzenleme atomik uygulandı. Doğrulama: her yeni desen `grep -c` = 1; kalan
`(pnl|win_rate|accuracy|target_pct|expected_price|take_profit|stop_loss) … || 0` = **0**.

**Üretim derlemesi (`next build`) — sandbox tuzağı:** İlk iki deneme
`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]` ile düştü. Sebep **kod değil**:
sandbox'ın node shim'i Next.js'in kendi `.next` ara dosyalarını silmesini engelliyor.
Derlemenin kendisi geçiyordu (`✓ Compiled successfully`, `Finished TypeScript`,
`✓ Generating static pages (24/24)`). Tam yeşil çıkış için `.next` önceden silindi
(gitignore'lu, 0 tracked dosya, 135M) + sandbox'sız çalıştırıldı → **exit 0, 24 route**.

**Doğrulama özeti:** `tsc --noEmit` 0 hata · `next build` exit 0 (24/24 route) ·
backend **465/465** yeşil. Çalışma ağacı temiz (otomatik commit: `77c7a4b`, 29 dosya).

## KAPSAM DENETİMİ — W1–W6 yetmiyordu (2026-09-12, gece)

"Partiler bitti = hepsi kapandı" varsayımı **yanlıştı**. Bulgu listesi tek tek tarandı:
denetimde **10 KRİTİK** vardı; W1–W6 bunlardan 7'sini kapatmıştı. **2 KRİTİK hiçbir
partiye atanmamıştı:** `V-01` (mutabakat) ve `B-01`/`B-02` (piyasa verisi). Ayrıca
**B_* alt sisteminin tamamı** (20 bulgu) hiçbir düzeltme raporunda geçmiyordu.

**Ders:** düzeltme planına değil, **bulgu listesine** güven. Parti sonunda "kalan var mı?"
diye ID bazında tarama yap.

## W7 — Cüzdan mutabakat paritesi (V-01 KRİTİK, V-13 ORTA)
- **V-01:** `preview_portfolio_reconcile` `auto_paper_trades`'i **tamamen yok sayıyordu**
  (ne realize PnL ne açık maliyet); `reconcile_portfolio` ikisini de katıyordu. İki adımlı
  onayda admin **yanlış bakiyeyi** onaylıyordu (ölçüldü: +2.003,00 TRY sapma).
  `virtual_wallet` TRY satırı **iki defter tarafından paylaşılır** — bu yüzden tek kaynak şart.
- Çözüm: `database._portfolio_reconcile_figures(conn, cutoff)` → preview ve apply **aynı** SQL.
  **V-13:** `init_db` açılış mutabakatı da iki bacağı katıyor (placeholder 4 → 6).
- Test: `tests/test_w7_reconcile_parity.py` (10). **Mutasyon kanıtı:** V-01 geri konunca
  3 test kırıldı (`80.0 != 50.0`). Rapor: `FIX_W7_mutabakat_paritesi.md`.

## W8 — Piyasa verisi & WS dayanıklılığı (B-01…B-06)
- **B-01 (KRİTİK):** 24s ömür dalı `reconnect_requested=True` yapıp `ws_connected_at`'i
  **sıfırlamıyordu** → her nesil yeniden tetikleniyor, watcher anında dönüyor, soketler el
  sıkışmadan iptal ediliyordu. Ölçüldü: **1,5 sn'de 17.854 nesil (~11.900/sn), 0 bağlantı**.
  Çözüm: `ws_connected_at = 0.0` + watcher'da **min-dwell** (kontrol sleep'ten SONRA) +
  `WS_GENERATION_MIN_INTERVAL_SEC = 0.25`.
- **B-02 (KRİTİK):** `main.py` REST yedeği REST'in **son satırının hâlâ açık mum** olduğunu
  yok sayıp `timestamps`/`last_closed_at_ms` **yazmadan** önbelleğe yazıyordu →
  `kline_freshness` `age=inf` → sembol **kalıcı fail-closed**. Çözüm: `market._closed_history(...)`
  (kanonik; `routers/runtime.py:920` ile aynı). + `_process_kline`'a desenkron seri onarımı.
- **B-03:** `plan["url"]` donmuştu → yedek host hiç denenmiyordu. `_ws_url_for` ile her
  denemede yeniden kur + `WS_BASES` rotasyonu + üstel backoff/jitter (`_ws_backoff_sec`, 30 sn tavan).
- **B-04:** `_ticker_params` `symbols[:50]` ile evreni **sessizce kesiyordu**; `ticker_24h`
  **replace** ediliyordu → 50 dışı semboller hacmini kaybedip **sessizce** kilitleniyordu.
  Çözüm: `_ticker_paged()` dilimle+birleştir; `ticker_24h.update(...)` → merge.
- **B-05:** tek bozuk çerçeve tüm grup soketini düşürüyordu → `_handle_ws_frame()` çerçeve
  başına koruma + `ws_malformed_frames` sayacı.
- **B-06:** `create_task(repair_history_gaps(...))` sonucu tutulmuyordu (GC riski, istisna
  görünmez) → `_schedule_repair()` + `_bg_tasks` güçlü referans + done callback.
- Test: `tests/test_w8_market_data_fixes.py` (18). **Mutasyon kanıtı:** B-01 ve B-04 geri
  konunca testler kırıldı. Rapor: `FIX_W8_piyasa_verisi.md`.
- **Yan etki:** `test_market_data_behavior.py` nesil rotasyonunu 0,5 sn'de bekliyordu;
  min-interval eklendiği için sabit 0,25 sn'ye çekildi + test penceresi genişletildi
  (testin amacı değişmedi).

## ARAÇ HATASI — Git Bash ters bölü bozuyor (ÖNEMLİ)
`python - <<'EOF'` veya `python -c "..."` ile satır içi betik verirken **Git Bash ters
bölüleri bozuyor**: Python dizesindeki `\n` → `/n` oluyor ve `str.count()` **0 eşleşme**
döndürüyor. Belirti: mutasyon script'i sessizce hiçbir şey yapmadı, testler "geçti" göründü.
**Kural:** ters bölü/newline içeren betikleri **Write aracıyla dosyaya yaz**, sonra çalıştır.
(`apply_*.py` betikleri bu yüzden sorunsuzdu; yalnızca satır içi heredoc'lar bozuldu.)

## Durum (güncel)
Denetimdeki **10 KRİTİK bulgunun tamamı kapandı.** Test paketi **493/493 yeşil**
(381 → 416 → 442 → 465 → 475 → **493**). Açık kalan: B alt sisteminin 8 ORTA + 6 DÜŞÜK
bulgusu (B-07…B-20) ve backend **muhasebe** tarafını görüntüyle eşitleme.

---

## W9 + W10 — B alt sisteminin kalan bulguları (ORTA + DÜŞÜK)

**Amaç:** Denetim kapsam taramasında W1–W8'in B alt sisteminin tamamını kapatmadığı
görülmüştü. W8 KRİTİK+YÜKSEK'i (B-01…B-06) kapatmıştı; W9/W10 kalan **8 ORTA + 6 DÜŞÜK**
bulguyu (B-07…B-20) kapattı.

**Değişen dosyalar:** `backend/app/market_data.py`, `microflow.py`,
`binance_tr_public.py`, `binance_tr_private.py`, `universe_registry.py`,
`routers/reports.py`.

**Öne çıkan sessiz bozulmalar (düzeltildi):**
- B-07: 5m kline 10,5 dk "fresh" kalıyordu → MTF kapıları yanlış pozitif.
- B-08: 5s kova fazı akış başlangıcına bağlıydı; tek 1s bar düşünce 1000 ms kayıyordu.
- B-09: MicroFlow 120 sembolde 120 seri bellekte tutuyordu (kalıcı büyüme).
- B-10: tumbling CVD penceresi sınırda geçerli veriyi siliyordu (CVD %99,8 çöküyordu).
- B-15: `_interval_ms("1s")` 60_000 dönüyordu (60×), `"1M"` de 60_000 (43.200×).
- B-14: sunucu saati kayması tüm imzalı akışı kırabiliyordu; lot adımı uygulanmıyordu.
- B-20: `universe_at()` hiç çağrılmıyordu → hayatta kalma yanlılığı düzeltmesi ölü koddu.

**B-17 kapatılmadı, "tasarım gereği" işaretlendi.** Gerekçe: velocity havuzu ana evrenin
üst kümesidir (dedup yalnız kesişimde mümkün), `kline_1s` MarketData planında yok, ve
fan-out `MarketData` grup soketi düştüğünde velocity karar yolunu sessizce kör bırakırdı.
Bulgunun "iki görünüm tutarsız CVD" gerekçesi fan-out ile zaten düzelmiyordu; asıl çözüm
ufuk etiketleriydi (B-10/B-19 ile eklendi). Detay: `FIX_W9` raporu.

**Kanıt:** `test_w9_market_fixes.py` (23) + `test_w10_adapter_fixes.py` (18) = 41 yeni kilit.
`scratch/verify_w9_w10_mutations.py` ile **17/17 mutasyon yakalandı** (her düzeltme eski
hâline döndürülüp ilgili testin kırıldığı gösterildi).

**Test paketi: 493 → 534 yeşil, 0 kırmızı.**

**Raporlar:** `outputs/denetim_2026-09-12/FIX_W9_piyasa_mikro_akis.md`,
`FIX_W10_borsa_adaptorleri.md`.

**Araç notu (yeni):** `Path.read_text()` CRLF'i LF'e çevirir, `write_text()` geri
`os.linesep`'e — yani atomik betikler satır sonlarını bozmaz. Ancak dosya sonunda
newline YOKSA, `old` deseninin sonuna `\n` koymak eşleşmeyi 0 yapar (public adaptörde
bir kez bu tuzağa düşüldü).
