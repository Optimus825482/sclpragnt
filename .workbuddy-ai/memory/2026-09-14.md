# 2026-09-14

## Konsol hataları: `/api/chart/{SYM}/forecast` 503 + Binance TR WS uyarısı

Kullanıcı konsoldan iki hata yapıştırdı:
1. `wss://stream-cloud.binance.tr/ws/btctry@kline_5m` → "WebSocket is closed before the connection is established"
2. `/api/chart/BTCTRY/forecast` ve `/api/chart/XRPTRY/forecast` → 503 (tekrarlı)

### 2) 503 — KÖK NEDEN BULUNDU VE DÜZELTİLDİ (ölçüldü)

`app/routers/chart_forecast.py::collect_forecast_features()`:
- D-04 gereği oluşmakta olan 5m barı düşürür, sonra **son KAPANMIŞ 5m barın** yaşını ölçer.
- 5m grid'de bu yaş **doğal olarak 0..300 sn** arasında değişir, ama kapı sabit
  `last_age_sec > 180` idi → 5 dakikalık pencerenin son ~2 dakikasında **canlı sembolde
  bile** `None` dönüyor → endpoint 503.
- **Kanıt:** sahte saatle (`work/prove_forecast_freshness_bug.py`) 5 dk pencere tarandı;
  mutasyon kontrolü (`work/mutate_check_chart_forecast.py`): eski eşik 180 sn → **12/31
  örnekte** None (~%40), yeni eşik 420 sn → **0/31**.
- BTCTRY + XRPTRY'nin **aynı anda** 503 vermesi imzası: ikisi de aynı 5m grid'inde.

**Düzeltme:** eşik bar aralığına ölçeklendi
(`INFERENCE_BAR_MS=300_000`, `FRESHNESS_GRACE_SEC=120` → 300+120=420 sn).
Ölü sembol yakalama amacı korundu. Uygulama: `work/apply_chart_forecast_fix.py` (atomik, 3 düzenleme).
Not: `routers/velocity.py:326` aynı kalıbı **1m** seriyle kullanıyor (`59_999`, eşik 180)
→ orada yaş 0..60 sn olduğu için sorun yok, değiştirilmedi.

**Test:** `tests/test_chart_forecast.py` → `ChartForecastFreshnessTests`
(tam 5m pencere taraması + ölü sembol hâlâ reddediliyor + eşik aralıkla ölçekleniyor).
Mutasyonla doğrulandı. `pytest tests/ -q` **tam paket yeşil**, `npx tsc --noEmit` temiz.

### 1) WS uyarısı — ZARARSIZ, bastırıldı

- "closed before the connection is established" = `ws.close()` CONNECTING durumundayken
  çağrıldı. `next.config.js` `reactStrictMode: true` → dev'de effect çift çağrısı + her
  sembol/TF değişimi bunu üretir. **Gerçek bağlantı hatası değil.**
- Host erişilebilirliği doğrulandı: `stream-cloud.binance.tr` ve `stream.binance.me`
  TCP/443 OK.
- `frontend/app/charts/page.tsx` temizliği: CONNECTING ise kapatma `onopen`'a erteleniyor
  (`const pending = ws; ws.onopen = () => pending.close();`). `tsc --noEmit` temiz.

## Not

- Backend bu oturumda ayakta değildi (8000/8001/8004 bağlantı reddi) — teşhis
  modül düzeyinde canlı Binance TR verisiyle yapıldı (REST klines çalışıyor, artifact
  `D:\data\ml_models\upside_v3.joblib` yükleniyor, 308 sembol).
- MEMORY.md enjeksiyon limitini aşıyordu → sıkıştırıldı ve yeniden düzenlendi (12KB → 8.9KB).

## RADAR TESPITLERI analizi (13–14 Eylül, 104 kayıt)

Kullanıcı radar tablosunu yapıştırdı (14.09: 25, 13.09: 79). Kodla kolonlar teyit edildi
sonra `work/radar_tespitleri.json` + `work/radar_analiz.py` ile hesaplandı.
Rapor: `outputs/radar_tespit_analiz_2026-09-14.md`, görsel: `outputs/radar_rejim_2026-09-14.html`.

**Ölçüm tanımı (kod kaniti — bir daha aramaya gerek yok):**
- `monitoring.py:1327-1333` → TAMAMEN = `touched_target` (hedefe GERÇEK dokunuş),
  KISMI = MFE>0 dokunmadı, BAŞARISIZ = MFE<=0, ÖLÇÜLEMEDİ = pencere kapandı veri yok.
- `velocity.py:876-877` → `mfe_pct = _mfe_from_window(window, entry)` — 1m **high**'larından
  TEPE. Çıkış fiyatı / komisyon / slippage **YOK** → başarı olduğundan iyi görünür.
- `config.COMMISSION_PCT=0.0015` → gidiş-dönüş **%0.30**.
- `monitoring.py:45` `NOTIFY_COOLDOWN_SEC=300` = ufuk (5dk) ile AYNI → pencereler
  üst üste binmez ama **bitişik** (~7 dk aralık). Bağımsız örnek DEĞİL.
- `monitoring.py:67-81` `normalize_score = min(100, ham/2000*100)` → **kırpılıyor**.

**Bulgular (ölçüldü):**
- TAMAMEN 8/103 = **%7.8**; BAŞARISIZ 36/103 = **%35**; ortalama MFE +0.87%, medyan +0.31%.
- MFE > %0.30 olan yalnız **50/103** → tepeyi yakalasan bile yarısı komisyonu zar zor karşılar.
- **Skor 100.00: 100/104 (%96)** → eşik (SKOR≥90) ayırt edici değil, sıralama yapamıyor.
- **ML olasılık vs MFE: r = −0.050 (n=103), t = −0.50 → anlamsız.** ML≥%50 (n=11) ile
  ML≤%9 (n=38) arasında TAMAMEN oranı farkı yok (%9 vs %8). Sinyal taşımıyor.
- **ARKTRY tek başına 54/104 (%52).** 13.09: 33 bildirim → **0 TAMAMEN** (fiyat 9,45'te sabit,
  aynı fiyattan 9 kez tetiklenmiş, hepsi BAŞARISIZ). 14.09: 21 bildirim → 6 TAMAMEN
  (7,30→10,15, +%39 ralli). **Başarı rejimi ölçüyor, sinyalin kenarını değil.**

**Karar:** strateji kârlı mı → **BELİRSİZ** (MFE tepe + maliyetsiz). Başarı metriği
**YÖNTEM HATALI**. Hiçbir eşik/ağırlık değiştirilmedi (paper-only + kanit kuralı).
Öneri sırası: (1) başarı = gerçekleşen çıkış + komisyon + slippage, (2) ML olasılığı
skordan çıkar/yeniden kalibre et, (3) cooldown'u ufkun üstüne çıkar + aynı fiyatta
yeniden tetiklemeyi engelle, (4) skor kırpılmasını kaldır.

## DEVAM: gerçekleşen çıkış ölçümü + VERİ BÜTÜNLÜĞÜ ŞÜPHESİ (yeni bulgu)

104 tespitin her biri Binance TR 1m mumlarıyla yeniden kuruldu (`work/radar_exit_analiz.py`,
cache `work/radar_exit_cache.json`). Rapor: `outputs/radar_exit_analiz_2026-09-14.md`.

Yöntem: pencere `velocity._post_signal_window` ile (AYNI tanım); `mfe` = pencere en yüksek
high (ulaşılamaz tepe), `exit` = son kapanmış mumun close (gerçekleştirilebilir),
`net = exit − %0.30` (gidiş-dönüş komisyon).

**Mutabakat kapısı zorunlu:** tespit fiyatı pencerenin ilk bar aralığında olmalı VE
yeniden hesaplanan MFE tablodakiyle ±1 y.p. içinde olmalı. Sonuç:
**yalnız 15/104 (%14) mutabık** → kârlılık sayısı YAYINLANAMAZ, durum **BELİRSİZ**.

**YENİ BULGU (kârlılıktan bağımsız, daha acil): tablodaki fiyat piyasayla uyuşmuyor.**
- 13.09 ARKTRY gerçek 5m mum aralığı: **5,95 – 9,94**
- Tabloda 13.09 ARKTRY için yazan fiyatlar: **9,05 – 10,26** (gün maksını AŞIYOR)
- 13.09 23:13 gerçek ~7,0 iken tablo **10,26**
- Tablodaki 9,45 gerçekte **10:25**'te geçmiş (tabloda 05:31–06:18)
- 14.09 satırları büyük ölçüde mutabık → sorun 13.09'a özgü görünüyor.
Olası neden: bildirime yazılan `price` bayat/yanlış kaynak, `detected_at` ile fiyatın
farklı ana ait olması (zaman damgası kayması), veya görünümde tarih/saat dönüşüm hatası.

**Karar:** veri bütünlüğü şüphesi kapatılmadan bu tabloya dayanarak eşik/ayar
DEĞİŞTİRİLMEZ. Sıra: (0) fiyat kaynağı + zaman damgası incelemesi → (1) başarı metriği.

Mutabık alt kümede (n=15) yön net: MFE ort. **+1.75%** vs gerçekleşen çıkış **+0.02%**
→ tepe iyimserliği **1.73 y.p.**; net ort. **−0.28%**, net pozitif 5/15. Örneklem küçük.

## "hepsini sırayla tamamla" — 5 iş tamamlandı (D-05..D-08 + ML kilidi)

Rapor: `outputs/radar_duzeltme_raporu_2026-09-14.md`. Hepsi additive; strateji
aktive edilmedi (paper-only). Toplam +40 test, **15/15 mutasyon kırıldı**.

**⚠ VERİ KAYNAĞI:** kullanıcı tablosu 13–14.09; yerel `scalper` DB'sinde son kayıt
**07.09.2026** (42.773 aday). Tablo bu instance'dan üretilmedi → dağıtım yapılmadan
düzeltmeler kullanıcının ekranında görünmez. Ölçümler yerel örneklemden.

1. **D-06 gerçekleşen çıkış** — `exit_pct` (ufuk sonu close) + `net_pct`
   (gidiş-dönüş maliyeti sonrası). DB kolonları + API + rapor kolonu "Net (Cikis)".
   Tek kaynak maliyet: **`config.round_trip_cost()`** (yeni, kesir) — %0.35
   (komisyon 0.0015 + slippage 0.00025, iki bacak). Eski notlardaki "%0.30" YANLIŞTI
   (yalnız komisyon). 11 test / 5 mutasyon.
2. **ML olasılık (iş #3) — ETKİ YOKTU, KİLİTLENDİ.** Skora 2026-09-07'de zaten
   girmiyordu; hedefe de girmiyor. `ml_target_pct` giriyor ama **bağlayıcı 0/359**
   (ML medyan %0.38 bekliyor, radar %2-4 istiyor → 5× ölçek uyuşmazlığı).
   Olasılığın ayırt ediciliği: tablo r=−0.050; DB r=+0.095 (MFE) / +0.111 (dokunuş,
   t=2.13 sınırda) → **işaret bile uyuşmuyor**. Kalibre edilmemiş yeşil/sarı/kırmızı
   rozet **nötrleştirildi** (`frontend/app/lib/mlProbability.ts`, 3 dosya).
   AST kilidi `tests/test_ml_etki_kilidi.py` (2/2 mutasyon). `ml_target_pct` kanalı
   **korundu** (kanıtı var: r=+0.344, t=+6.91).
3. **D-07 yeniden tetikleme kapısı** — fiyat değişmediyse yeni bildirim yok.
   Eşik **%0.35 = gidiş-dönüş maliyeti**: 88 ardışık çiftte 26 tekrarı bastırıyor,
   **0 TAMAMEN kaybı**, TAMAMEN %8.0→%11.3. (0.50'de 1, 0.75'te 2 TAMAMEN kaybı
   başlıyor → 0.35 ampirik optimum.) 13 test / 4 mutasyon.
   Not: `test_m1_monitoring_fixes._reset_state` + `test_monitoring._reset_state`
   artık `notified_prices` da sıfırlıyor (kapı doğru çalıştığı için çakışma çıktı).
4. **D-08 skor doygunluğu** — 104 tespitte 5 farklı skor, %96.2'si tam 100.00.
   **CAP DEĞİŞTİRİLMEDİ**: hedef bantları panel ölçeğinde → cap değişimi her
   adayın hedefini değiştirir = strateji değişimi (OOS kanıtı yok).
   Yerine: ham `velocity_score` rapor API'sine taşındı + `saturated` bayrağı +
   "Ham Skor" kolonu. 10 test / 4 mutasyon (`normalize_score` kırpma davranışı
   bilinçli olarak kilitli — değiştirilirse strateji kayması olur).

**Hedef kalibrasyonu OOS (`work/hedef_kalibrasyon_oos.py`,
`outputs/hedef_kalibrasyon_OOS_2026-09-14.md`) — KENDİ HİPOTEZİMİ ÇÜRÜTTÜ:**
- Skor MFE'yi gerçekten seçiyor: en üst/en alt decile MFE≥%2 oranı **4.13×**
  (%9.3 → %38.3). "Hedef gürültüyü ölçüyor" iddiası YANLIŞTI → raporda düzeltildi.
- Ölçüm hatam: `ort. hedef × ort. isabet` koşullu politikada geçersiz
  (korelasyonu yoksayar). Doğrusu kayıt bazlı `EV = ort(hedef × 1{MFE≥hedef})`:
  MEVCUT **+0.346**, decile-kalibre **+0.360**, en iyi SABİT (%1.00) **+0.341** y.p.
  → mevcut politika en iyi sabit hedefi geçiyor; kalibrasyon **+0.015 y.p. (%4.3)**
  = gürültü → **aktive edilmedi**.
- Zaman eğilimi asıl hikâye: ort. hedef %2.49→%2.56→%1.80→**%1.43**, isabet
  %13.9→%8.3→%21.8→**%30.4**. Adaptive hedefler sorunu zaten büyük ölçüde çözmüş;
  ilk turdaki "%9.2 dokunuş" bulgusu DÖNEME ÖZGÜYMÜŞ.
- `exit_pct` GERİYE DÖNÜK doldurulamaz (`outcome_details` pencereyi saklamıyor,
  yalnız `window_bars`) → D-06 kolonları sadece ileriye dönük dolar.
