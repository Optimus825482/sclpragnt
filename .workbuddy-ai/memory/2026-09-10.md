# 2026-09-10

## Uygulama kapsamlı analiz raporu
- Scalper Agent V4 (paper-trading Binance TR scalping bot) kaynaktan incelendi.
- Rapor: `outputs/uygulama_kapsamli_analiz_raporu.md` (TR, dosya:satır kanıtlı).
- 5 paralel alt-inceleme: çekirdek motor, portföy/DB/state, API router'ları, learning/ML, frontend hesapları.
- Kritik bulgular (kaynaktan doğrulandı):
  - velocity otonom döngüsü ölü (`routers/velocity.py:1506-1514`): bool klines yerine kullanılıyor, sonuç atılıyor → hiç açış yok.
  - `reconcile_portfolio` auto_paper_trades'i yok sayıyor → paylaşılan TRY cüzdanı bozulabilir.
  - devre kesici `is_paused()` hiç çağrılmıyor → duraklatılan strateji yine BUY alıyor.
  - korelasyon izleyici hiç tazelenmiyor → maruziyet cap'i sabit 0.75.
  - README'daki BB_MFI stratejisi 2026-09-04 çıkarılmış; canlı stop %1.2 / TP %1 (config.py:236,245).
  - CRSI percent-rank yanlış; chart_forecast direction_correct her fitili sayıyor; MFI/OBV/pattern frontend parity bozuk.
- Öneri: Aşama 1 (güvenlik/kontrol) + Aşama 2 (hesap doğruluğu) hemen ele alınmalı.

## Tüm bulgular düzeltildi + yeniden analiz (aynı gün)
- Rapor `outputs/uygulama_kapsamli_analiz_raporu.md` "Bölüm 10" ile güncellendi (düzeltme + doğrulama kanıtı).
- Düzeltilen dosyalar (backend): analyzer.py, database.py, circuit_breaker.py, technical_analysis.py,
  calibration.py, market_data.py, ml_forecast.py, chat_pattern_replay.py, forecast_learning.py,
  market_intelligence.py, memory_service.py, agent_learning.py, config.py, main.py,
  routers/{velocity,runtime,chart_forecast}.py, README.md
- Düzeltilen dosyalar (frontend): app/charts/{signals,chartShared}.ts + page.tsx,
  app/{history,portfolio,macd-monitor}/page.tsx, app/lib/format.ts
- **Yeniden analizde bulunan 5 EK hata (önceki turda kaçmış):**
  - E1: `predict_target` çağıranları arasında `ret3_pct` birimi tutarsız (kesir vs yüzde) → 100x hata.
    Sözleşme tekilleştirildi: TÜM `*_pct` alanları YÜZDE; ml_forecast içeride kesire çevirir (`_ratio_from_pct`).
  - E2: `velocity_proxy` eğitimde `atr*100*(1+ret3)`, çıkarımda `ret1` → özellik kayması.
    Paylaşılan `velocity_proxy_value()` üç yolda kullanılıyor.
  - E3: `prepare_journal_samples` düz snapshot bekliyordu; bazı yazıcılar `{"candidate":{...}}` kaydediyor
    → journal örnekleri boş özellikle eğitime giriyordu. İç içe yapı geri düşmesi eklendi.
  - E4: `/api/ml/predict` boş özellikle (`predict_target(symbol,{})`) çağırıyordu → gerçek canlı özellikler.
  - E5: `app/lib/format.ts` fiyat hassasiyeti kanonik kuralla çelişiyordu → hizalandı.
- **Kalan bilinçli risk:** eğitim (5m mum: SMA-RSI, Aroon-14) ile çıkarım (velocity: Wilder-RSI, Aroon-25)
  gösterge tanımları farklı. Velocity eşikleri mevcut implementasyona kalibre olduğundan
  replay/OOS doğrulaması olmadan DEĞİŞTİRİLMEDİ. Öneri: göstergeleri tek modülde birleştir + replay ile yeniden kalibre et.
- Doğrulama: 272 backend testi geçti; `tsc --noEmit` exit 0; py_compile exit 0;
  ADX trend=100/chop=16.02; CRSI tırmanış=100/düz=33.33; MFI düz=100; saat hizası 200k örnekte 0 uyumsuzluk;
  eğitim/çıkarım satırı ret3/atr/mfi/velocity_proxy/hour eşleşti.
- Tekrarlanabilir iş akışı `code-audit-fix-verify` skill'i olarak kaydedildi (user-level).

## İkinci tur bağımsız denetim — daha önce bakılmayan alanlar (aynı gün)
- Odak: eşzamanlılık/canlılık, kalıcılık bütünlüğü (şema vs kod), API sağlamlığı, para matematiği, hata yönetimi, frontend renk anlamı.
- **10 bulgu** kaynakta doğrulandı ve düzeltildi (rapor **Bölüm 11**):
  - F1/F2: `trades.trade_id` ve `auto_paper_trades.notification_id` indeksleri yoktu; ikisi de sıcak yolda sorgulanıyor
    → `backend/migrations/001_pgvector_schema.sql` (`idx_trades_trade_id`, `auto_paper_trades_notification_idx`).
    Not: şema SHA-gate'li init_db ile uygulanır; SQL dosyası değişince şema yeniden koşar → indeksler oluşur.
  - F3 (en önemli): dashboard `total_value` açık `auto_paper_trades` pozisyonlarını **hiç** katmıyordu
    → `database.py::get_dashboard_summary`. Canlı DB'de sapma **7.233,11 → 13.283,32 ₺** (fark **6.050,21 ₺**).
    Yeni alanlar: `auto_paper_open`, `positions_value`, `auto_paper_value`, `basis`.
  - F4/F5: frontend'de `null` PnL YEŞİL boyanıyordu (`portfolio/page.tsx`, `reports/page.tsx`),
    ve özet yüklenmeden (`s==null`) dashboard PnL'i KIRMIZI görünüyordu (`page.tsx`) → hepsi nötr `text-bunker-muted`.
  - F6: `refresh_top_gainer_symbols` ağ I/O'sunu (`ticker_24h`, `trading_symbols`) `_top_gainers_lock` içinde
    yapıyordu → ağ çağrıları kilit dışına alındı (`routers/runtime.py`).
  - F7: `analyzer.py` giriş sıcak yolundaki koşulsuz debug `print` kaldırıldı.
  - F8: admin tablo listesinde N+1 `COUNT(*)` → tek `UNION ALL` (`main.py`).
  - F9: admin export sınırsızdı → `_ADMIN_EXPORT_MAX_ROWS=20000` + `X-Export-Truncated`/`X-Export-Rows` başlıkları.
  - F10: `DEFAULT_ORDER_TRY` takma adı eklendi (eski `DEFAULT_ORDER_USDT` geriye dönük korunuyor).
- **Değiştirilmeyen (gerekçeli):** `_radar_lock` (main.py) ağ taraması boyunca tutuluyor ama bu bilinçli
  single-flight deseni (çift kontrollü önbellek); kaldırmak pahalı taramayı çoğaltırdı.
- Doğrulama: 272/272 backend testi; `tsc --noEmit` exit 0; py_compile + import temiz.
  **Canlı PostgreSQL:** UNION ALL 52/52 tablo 0 uyuşmazlık; `idx_trades_trade_id` → `Index Only Scan`;
  `auto_paper_trades_notification_idx` oluştu ve kullanılabilir (tablo 3 satır → planlayıcı seq scan seçiyor, doğru davranış).

## Tur 3 — Ertelenen 3 maddenin tamamlanması (2026-09-10, akşam)

Kullanıcı "Ertelenen 3 maddeyi de tamamla" dedi → E6, Madde 14, Madde 21 kapatıldı. **Yol haritası 21/21.**

- **Madde 14 — monkeypatch → açık bağlama:**
  - Yeni `backend/app/runtime_deps.py`: `pending_dep(name, required=True)` (yer tutucu; çağrılırsa açıklayıcı `RuntimeError`,
    sessiz `NameError` yerine), `bind(module,name,handler)` (bağlamayı KAYDEDER), `missing()`, `assert_ready(strict=True)`,
    `status()`, `reset()`.
  - `routers/runtime.py` + `routers/llm_chat.py`: `pending_dep(...)` yer tutucuları.
  - `main.py`: 6 geç bağlama artık `runtime_deps.bind(...)`; `startup_services()`'in **ilk satırı** `runtime_deps.assert_ready()`.
  - Test: `tests/test_runtime_deps.py` (6). Üretim bağlamaları `__module__ == "app.main"` ile doğrulanıyor (boş yere geçemez).
  - Not: `bind` hedefi attribute kabul eden modül olmalı — testte `object()` kullanılamaz (setattr → `no __dict__`), namespace sınıfı gerekir.
- **E6 — ML gösterge tanımı birleştirme:** kanonik `technical_analysis._linreg_slope_pct` (10-bar, yüzde), `_aroon`
  (high/low, `period+1=26` pencere, **son-tekrar tie-break**), `_wilder_rsi_series`. `ml_forecast.py` bunları kullanır;
  `FEATURE_NAMES` `aroon_up25/aroon_down25`; `FEATURE_VERSION` **v2→v3** (eski artifact'lar geçersiz → retrain zorunlu).
  4. uyumsuzluk bulundu: Aroon eğitimde kapanış, çıkarımda high/low'dan hesaplanıyordu.
  OOS: **1.411.529 pencere / 306 sembol → 0 fark** (tie-break düzeltmesiyle 31,7%→1,44%; `period+1` ile →0).
  Test: `tests/test_ml_feature_parity.py` (8).
  KURAL: Aroon tie-break **son görülen** zirveyi/dibi tercih eder (yeniden test edilen tepe = 0 bar önce).
- **Madde 21 — ölü kod temizliği (her kaldırma grep ile kanıtlandı; app+scripts+tests+work+frontend+docs tarandı):**
  - Kaldırıldı: 4 config sabiti (`EXIT_ON_OPPOSITE_SIGNAL`, `MAX_POSITION_LAYERS`, `MONITORING_MICRO_CVD_POSITIVE_MULT`,
    `SYMBOL_PYRAMIDING_LAYERS`); `analyzer._flow_filter`+`_optional_flow_filter` (**ayrıca `config.ORDERFLOW_MIN_IMBALANCE`
    yoktu → çağrılsa AttributeError**); `_last_signal_lengths` (+ 3 `.clear()`); `_all_same`; `_profile_prefix`.
  - `calculate_atr` ölü `period=11` varsayılanı → `14` (15 çağıranın hepsi açıkça 14/SYSTEM_ATR_PERIOD geçiyordu).
  - `DEFAULT_ORDER_TRY` artık `config.min_net_exit_pct`'te okunuyor (canlı).
  - **`load_positions` yazma yan etkisi ayrıldı:** legacy `trade_id` UPDATE+COMMIT okuma yolundaydı →
    yeni `database.backfill_position_trade_ids()`, `init_db()`'den bir kez çağrılıyor. Okuma yolu artık yazmıyor
    (eksikse yalnız bellek-içi geçici kimlik). Test: `tests/test_database_purity.py` (4).
  - **Yanlış-pozitif olarak ELENENLER (silinmedi, gerçek işlevleri var):** `migration_monitor` (canlı: system.py/runtime.py+testler),
    M1 flat-candle filtreleri (**tam uygulanmış**: `runtime.py::_m1_flat_candle_activity` + kapı + testler; sadece varsayılan kapalı),
    `VELOCITY_TRAIL_GAP_PCT` (canlı: `work/chat_replay_6h.py`), `backend/scripts/*` (araştırma araçları; 202 dosya → 0 derleme hatası).
- **YENİ BULGU (karar bekliyor) — `autonomous_velocity_loop` bağlanmamış:** `velocity.py:1484` döngü tam uygulanmış
  (M5 kapanış senkronu, çift kilit) ama `startup_services()`'te `_start_background` ile **başlatılmıyor**.
  `backend/.env`'de `VELOCITY_AUTO_ENABLED=true` olmasına rağmen çalışmıyor; `/api/velocity/status` `auto_enabled`'ı
  env'den `true` raporluyordu → **UI yanıltıcıydı**. **Bilinçli olarak aktive edilmedi** (otonom paper = strateji
  aktivasyonu; kullanıcı kuralı: OOS kanıtı olmadan aktive yok). Bunun yerine: döngüye ⚠️ BAĞLANMAMIŞ KANCA notu +
  tek satır aktivasyon talimatı; status ucuna dürüst **`loop_running`** alanı (`_VELOCITY_AUTO_LOOP_STARTED`, şu an false).
  Aynı şekilde `invalidate_wallet_caches` (runtime.py; TTL=3sn olduğu için bayatlık sınırlı),
  `_ma_cascade_observation_context`, `_persist_replay_parity_observation` silinmedi → işaretlendi.
- Doğrulama: **290/290** backend testi (272 → +8 E6 → +6 runtime_deps → +4 db purity); `tsc --noEmit` exit 0; py_compile temiz.
- Rapor: `outputs/uygulama_kapsamli_analiz_raporu.md` → Bölüm 12 eklendi; Bölüm 0/7/9 güncellendi (yol haritası 21/21).

## Tur 3b — `autonomous_velocity_loop` AKTİVE EDİLDİ (kullanıcı kararı)

Kullanıcı: **"autonomous_velocity_loop AKTİVE EDELİM"** dedi → döngü bağlandı.

- **Değişiklik (`app/main.py`):**
  - import: `from app.routers.velocity import velocity_learning_loop, autonomous_velocity_loop, load_velocity_atr_profiles`
  - `startup_services()` içine: `_start_background(autonomous_velocity_loop, "velocity-autonomous")`
    (mevcut `_start_background` süpervizörü: sonlu backoff ile yeniden başlatma; `strategy_loop`/`radar_loop` ile aynı desen).
    Ek `*_start_loop` sarmalayıcısı GEREKMEDİ — döngü kapıyı her turda kendi okur.
- **Kapı durumu (aktivasyon anında, ikisi de AÇIK):** `VELOCITY_AUTO_ENABLED=true` (backend/.env),
  `llm_paper_trade_enabled=1` (llm_settings DB). → Bir sonraki M5 kapanışında tarama başlar, paper pozisyon açabilir.
  Kapatma: env `false` **veya** DB `0` (restart gerekmez). Paper-only, gerçek emir yok.
- **Yeni koruma — `tests/test_loop_wiring.py` (2 test):** `app/` altındaki her `*_loop` tanımı ya `main.py`'de
  geçmeli ya da kendi modülünde `create_task(...)` ile başlatılmalı. `_ALLOWED_UNWIRED` boş.
  **Mutasyonla doğrulandı:** wiring çıkarılınca test kırılıyor (boş test değil).
  Bu testle, "döngü tanımlı ama başlatılmamış" sessiz hata sınıfı kalıcı olarak kapatıldı.
  Not: `auto_paper_management_loop`, `macd_monitor_loop`, `monitoring_background_loop` da main.py'de
  görünmez ama `*_start_loop` süpervizörleri + modül-içi `create_task` ile başlatılıyor → doğru kabul edilir.
- **Docstring/status güncellendi:** `autonomous_velocity_loop` docstring'i artık "AKTİF" diyor;
  `/api/velocity/status` → `auto_enabled` (ayar kapısı) ile `loop_running` (fiilen başlatıldı mı) ayrı alanlar.
- **Runtime kanıtı:** `_start_background` ile görev `velocity-autonomous` adıyla oluştu; `loop_running` False→True;
  temiz iptal; `runtime_deps` bağlı ve `missing: []`.
- Doğrulama: **292/292** backend testi; `tsc --noEmit` OK; py_compile temiz.

## Tur 4 — MACD MONITOR denetimi (salt analiz, kod değişmedi)

Kullanıcı: "MACD MONITOR sayfasını ve buradaki fonksiyonları kontrol et, eksik/hatalı var mı, hassasiyeti nasıl artırırız."
Rapor: **`outputs/macd_monitor_denetim_raporu.md`** (TR, file:line kanıtlı). **Hiçbir dosya değiştirilmedi.**

- İncelenen: `routers/macd_monitor.py` (790), `app/macd-monitor/page.tsx` (450), `lib/macdAccess.ts`,
  `settings/page.tsx` MACD paneli (893-977), `monitoring/page.tsx` (YÜKSELİŞ/SIRÇRAMA panelleri),
  `technical_analysis.py:_macd/_ema/_ema_series` (39-68, 6-13, 153-161), `market_data.py:refresh_series` (357-371).
- **A1 (🔴 yüksek) — M3/M30 REST tazelemesi hücrelere yansımıyor:** `_compute_pass` fiyat değişmediyse
  `continue` (`:580-581`) → refresh (`:556-573`) sonrası `_compute_cell("3m"/"30m")` yenilenmez. Tam da
  WS aboneliği olmayan (az tick alan) sembollerde ısrar eder. Trend skoru tazelenmiş veriyi görür, oklar görmez.
  Fix: tazelenen `(sym,tf)` için recompute'u zorla; `_last_price_seen`'i hesaplamadan SONRA yaz.
- **A2 (🟠) reentrancy yok:** `GET /api/macd-monitor` her istekte `_compute_pass(0)` (`:733`) → kilit yok, alarm
  tetikleyebilir, WS serileştirmesi sırasında `_SNAPSHOT["symbols"]` değişebilir. Fix: REST önbelleği döndürsün + `asyncio.Lock`.
- **A3 (🟠) trend gücü yönsüz:** `speed = abs(slope)/span` (`:239`) → `raw ≥ 0`; `_jump_score` yönsüz `+30`'a kadar
  ekliyor (`:414`); UI `arrowShade(row.tier)` güçlü DÜŞÜŞÜ koyu yeşil boyuyor (`page.tsx:78-82,367`). Yön eklensin.
- **A4 (🟡) evren-içi min-max (`:647-648, 521-525`)** → `monitoring/page.tsx:59` `RISING_MIN_STRENGTH=9.8` pratikte
  "evren maksimumu" → panel üyeliği kompozisyona bağlı, alarm titremesi. Mutlak ölçeğe (ATR% normalizasyon) geçilmeli.
- **A5 (🟡)** `weight_sum += weight` koşulsuz (`:614`) → `speed is None` olan TF ortalamayı seyreltiyor (veri eksiği = düşük skor).
- **A6 (🟡)** hacim/vol-state kapalı bar (`:301`,`:265-292`) ama kırılım canlı fiyat (`:257`) → jump skoru zaman-uyumsuz bileşen topluyor.
- **A7 (🟡)** her ~1 sn TAM snapshot WS yayını (`:713-716`) → ~300 sembolde ciddi bant genişliği; delta yayınına geçilmeli.
- **A8/A9/A10/A11/A12/A15 (⚪)** ticker'sız sembol donuyor; tick yokken bar kapanışı tazelenmiyor; GET `_SNAPSHOT`'ı mutasyona
  uğratıyor (`:737`); bayatlık uyarısı yalnız WS açıkken (`page.tsx:232`); ayar paneli ilk render "Kapalı" gösteriyor
  (`settings:943,950,957`); per-sembol hata izolasyonu yok (`IndexError` tüm turu keser).
- **A14 ✅ kontrol edildi, hata yok:** `_jump_alerted_at` ve `_early_alerted_at` ayrı sözlükler → jump/erken alarm birbirini bloke etmiyor.
- **A13 ⚪** MACD için hiç test yok; `test_loop_wiring.py:18` sadece adını anıyor. 6 maddelik test listesi raporda.
- ✅ Yöntemsel: `_MACD_MIN_CANDLES = 26+9 = 35` ile `_macd` kapısı tutarlı; `_macd` tüm uygulamada tek kaynak (parite OK).
- Öneri sıralaması (rapor Bölüm 3-4): **C1** saf doğruluk düzeltmeleri (A1/A2/A8/A9/A10/A11/A12/A15 + testler, davranış
  değiştirmez) → hemen yapılabilir; **C2** sinyal kalitesi (yönlü güç, mutlak normalizasyon, histerezis) → **OOS/replay şart**;
  **C3** alarm→sonuç isabet ölçümü (paper-only) → `jump_min_score=60` ve `_TF_WEIGHTS` ancak bununla ampirik ayarlanır.
- Karar bekliyor: C1 onayı, C2 için OOS önce mi, C3 başlatılsın mı, eşik/ağırlıklara şimdilik dokunulmasın mı.

## Tur 5 — MACD MONITOR C1–C4 uygulandı + OOS kanıtı (kod DEĞİŞTİ)

Kullanıcı onayı: "1 EVET · 2 HANGİSİ MANTIKLIYSA · 3 EVET · 4 GEREKİYORSA DOKUNULSUN · hepsini onaylıyorum."

**C1 — Doğruluk temeli (A1/A2/A8/A9/A10/A15):** `routers/macd_monitor.py`
- A1: `_compute_pass_locked` içinde `refreshed: set[(sym,tf)]` toplanır; tazelenen hücre fiyat değişmese de yeniden hesaplanır.
- A2: `_pass_lock` (asyncio.Lock) + `_REST_CACHE_MAX_AGE_SEC=5` → REST ucu taze önbelleği döndürür, çift hesap/alarm yok.
- A8/A9: per-`(sym,tf)` `_bar_marker()` → yeni bar kapanınca hücre tazelenir; ticker'sız sembol donmaz.
- A10: `_snapshot_payload()` sığ kopya (global `_SNAPSHOT` dışarı verilmez).
- A15: `_aligned_ohlc()` (hizalı OHLC kapısı) + `_symbol_trend_and_signals()` + per-sembol try/except.
- Frontend: `macd-monitor/page.tsx` bayatlık artık `dataAgeSec` + mod bazlı eşik (WS 15sn / REST 45sn);
  `settings/page.tsx` MACD panelinde `loaded/pending` → ilk render yanlış durum göstermiyor.

**C2 — Kanıt sonrası karar (B1 REDDEDİLDİ):** `work/macd_score_replay.py` + `work/macd_score_replay_sonuc.json`
- **312 sembol · 13.481 kesit · 571.980 gözlem** (65 gün 5m, sızıntısız).
- Kesitsel IC: `old_raw` (**mevcut, yönsüz**) **+0.0046/+0.0047/+0.0047** (5m/15m/30m, t≈**+2.4**) — zayıf POZİTİF.
  `new_raw` (B1 önerisi `max(0,eğim)`) **−0.016/−0.039/−0.049** (t −8.2/−19.3/−23.8). `dir_raw` (işaretli) **−0.025/−0.049/−0.060** (t −12.6/−23.8/−29.0).
- → **Yönlü momentum bu evrende CONTRARIAN; B1 güçlü kanıtla reddedildi.** `abs(slope)` KORUNDU, yön skora EKLENMEDİ.
- **B2 (mutlak ölçek) UYGULANMADI:** min-max kesit içinde monoton → IC birebir aynı (+0.0047); ölçülmüş fayda yok.
  Kalibrasyon kayıtlı: `absolute` p90=0.2081 → K_p90=**48.05**; `directional` p90=0.1380 → K_p90=**72.45**.
- **`jump_min_score`/`_TF_WEIGHTS` DEĞİŞMEDİ:** replay skoru yalnız güç+yeşil yarısını (maks 50/100) modelliyor;
  M5/M15 kırılım+genişleme+hacim+CVD (50 puan) modellenmedi → kısmi modelden ağırlık değiştirmek yasak ilkeyi ihlal eder.
  60/70/80 eşiklerinde satır çıkmaması **yapısal artefakt** (replay skoru 50'de tavan yapıyor) — üretim kanıtı DEĞİL.
- **Yeni `dir` alanı** (işaretli trend yönü) snapshot'a eklendi: **skora girmez**, tanımlayıcı/kanıt amaçlı.
- **B6 histerezis** (pay=5) korundu; boot sessiz, eşik çevresinde titreme alarm basmaz.
- Kanıt dokümanı: **`outputs/macd_monitor_replay_kanit.md`**.

**C3 — Alarm→sonuç kanıt katmanı (tamamlandı):**
- Tablo `macd_monitor_alerts` (migration `001_pgvector_schema.sql`, 13 kolon + 3 indeks) — **canlı DB'ye uygulandı ve doğrulandı**.
- `database.py`: `record_macd_monitor_alert` / `list_macd_monitor_alerts` / `fill_macd_monitor_alert_outcomes` (5m mumlarından 5/15/30m ileri getiri; 32 dk sonra `expired`) / `macd_monitor_alert_stats`.
- Kayıt: `_record_alert_evidence()` (jump+early), hata alarm akışını bozmaz. Doldurma: `macd_evidence_loop()` (~2 dk, `start_macd_monitor_loop` içinde `create_task`).
- API: `GET /api/macd-monitor/alerts?limit=&symbol=&days=` → `{alerts, stats}`.
- UI: "ALARM GEÇMİŞİ & İSABET" paneli (isabet özeti + alarm geçmişi; kâr yeşil / zarar kırmızı).
- Uçtan uca test edildi: insert→list→fill→stats→cleanup turu çalışıyor.

**C4 — Performans (B8/B9):**
- **B8:** `_trend_cache` — fiyat/kapanmış bar değişmedikçe 6 TF × (OLS+ATR+kırılım+hacim) yeniden hesaplanmaz; min-max normalizasyon her turda yenilenir.
- **B9:** `macd_monitor_delta` WS mesajı — yalnız değişen semboller; her 5. pass'ta TAM yayın (istemci onarımı); evren değişirse tam yayın zorunlu. Frontend delta'yı mevcut görünüme birleştirir.

**Doğrulama:** `tests/test_macd_monitor.py` **30 → 34 test** (TrendCache 3, AlertEvidence 2, AlertEndpoint 1, DeltaBroadcast 3);
tüm suite **316 → 325 test, hepsi geçti**; `tsc --noEmit` exit 0; `py_compile` temiz; migration canlı DB'de doğrulandı.
Rapor güncellendi: `outputs/macd_monitor_denetim_raporu.md` §7–8 (uygulama durumu + nihai beyan).

### B9 regresyonu — ikinci tüketici unutuldu (yakalandı ve düzeltildi)

**B9 delta yayını bir REGRESYON yarattı:** `macd_monitor_delta` eklenince, tam snapshot artık yalnızca
her 5. pass'ta (`_FULL_BROADCAST_EVERY`) + evren/ayar değişiminde gidiyor. `macd-monitor` sayfasına delta
desteği eklendi ama **`/monitoring` sayfası yalnız `macd_monitor` dinliyordu** → o panelin WS tazelemesi
**~1 sn → ~5 sn**'ye düştü (REST poll 15 sn olduğu için sessiz yavaşlama; hata vermez).
Ders: **yeni bir WS mesaj tipi eklerken TÜM tüketicileri grep'le** — `grep -rn "macd_monitor"` iki sayfa
gösterdi, yalnız birine destek eklemek yetmiyordu.

**Düzeltme (tek kaynak):** `frontend/app/lib/macdSnapshot.ts` → `mergeMacdDelta(prev, incoming)`;
hem `macd-monitor/page.tsx` hem `monitoring/page.tsx` bu tek tanımı kullanır.
Davranış testi (derlenmiş gerçek modül, 6/6 geçti): taban korunur · taban yoksa `null` (çökmez) ·
`symbols` yoksa değişmez · taban MUTASYONA uğramaz (React) · boş `universe/timeframes` tabanı silmez ·
**`jump_min: 0` yok sayılmaz** (`??` kullanıldı; `||` olsaydı 0 falsy tuzağına düşerdi).
Doğrulama: `tsc --noEmit` exit 0; backend odaklı test seti geçti. Gerekli olmayan geçici derleme dizini silindi.


### Erken sıçrama sinyali — zayıf nokta analizi + ölçüm enabler'ı

Kullanıcı sorusu: "MACD MONITOR'de hangi kısımlar geliştirilebilir, daha doğru/hassas ERKEN sıçrama sinyali için?"

**Teşhis:** erken alarm `pre_any = approach VEYA m1 VEYA dip` (macd_monitor.py:709-711) — üç öncünün
**eşit ağırlıklı boolean VEYA**'sı; skor yok, sıralama yok, ölçüm yok. Zayıf noktalar E1–E10 olarak
belgelendi (E1 ölçüm yok · E2 `dip` iki nokta karşılaştırması, kalıcılık/büyüklük/derinlik yok ·
E3 `m1` tek tick kırılımı, hacim/üst-TF vetosu yok · E4 `squeeze→expand` geçişi kullanılmıyor ·
E5 skor yok · E6 canlı↔kapanmış bar zaman tabanı uyumsuz · E7 yeşil oranı `_TF_WEIGHTS` ile çelişiyor ·
E8 `gap<=0.5` tek kademe · E9 sabit 30dk cooldown · E10 mikroyapı/CVD kullanılmıyor).

**Yol haritası:** `outputs/macd_monitor_erken_sinyal_yol_haritasi.md` (Aşama 1 ölçüm → 2 tanımlayıcı
alanlar → 3 kanıtlı sertleştirme (replay+paper şart) → 4 rejim koşullama). **Hiçbir sinyal davranışı
değiştirilmedi.**

**Uygulanan sıfır-risk enabler (Aşama 1/A1):** `database.py` `macd_monitor_alert_stats` →
`_alert_bucket_add`/`_alert_bucket_out` ile sadeleştirildi; `kinds` yanında **`precursors`** kırılımı eklendi
(`signals.early_signals` içindeki her etiket için ayrı kova; `isinstance(signals, dict)` koruması).
UI: `macd-monitor/page.tsx` paneli artık `GRUP` (TÜR/ÖNCÜ) + `AD` kolonları + `PRECURSOR_LABEL`.
Testler: `backend/tests/test_macd_alert_stats.py` (9 test) + `test_macd_monitor.py` → 42/42 geçti.
Canlı doğrulama: kontrollü satırlarla `dip` hem "jump" hem "early"da sayıldı, toplam uyuştu.

**Not (dürüstlük):** taban/lift (A2) ve olay çalışması (A3) henüz YOK — `avg_pct` tek başına iyi/kötü
ayrımı yapamaz. Sıradaki en yüksek getirili adım A1+A2.
