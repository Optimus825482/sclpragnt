# 2026-09-11 — Scalper Agent V4

## MACD MONITOR — Aşama 2 tamamlandı (task #34 ✅)
- `pre_detail` / `early_score` / `as_of` / per-(symbol,precursor) cooldown eklendi; `_early_trigger` kapısı.
- 76 test yeşil (test_macd_monitor.py + test_macd_alert_stats.py).
- Son kalan tek test fixture'ı düzeltildi: proximity fixture'ı prior_high'u seriden türetiyor.

## Aşama 3 ÖNKOŞULU — Erken öncü replay kanıtı (task #35 ✅)
Yeni betik `work/early_precursor_replay.py` (sızıntısız, 312 sembol, 13.487 kesit, ~65 gün). Sonuçlar:
- `outputs/erken_oncu_replay_kanit.md`.
- **Kritik bulgu:** `macd_dip_turn` üretimde ASLA 1 olamıyor (`_dip_detail` → `_macd`'a 34 bar, `_macd` ≥35 ister).
- `approach` ve `m1` bu evrende **ters (contrarian)**: OOS lift −0.08 / −0.09.
- `dip` (niyet) tek pozitif lift + en uzun lead (OOS +0.018).
- early_score sürekli değeri kesitsel IC **NEGATİF** (OOS −0.072, t −28): skor olarak UYGULANAMAZ, yalnız tanımlayıcı.
- Karar: C1(dip) düzeltilip tanımlayıcı; C2/C3/C5 REDDEDİLDİ; C4/C6/C8 veri yok; C7 adaptif cooldown gated.

## Evren/metrik not
- Evren 30m ortalama getiri ≈ +0.030%. Round-trip maliyet ≈ %0.35 (komisyon 0.15×2 + kayma 0.025×2).

## Aşama 3 KAPISI UYGULANDI (task #36, ilk parça ✅)
`_maybe_fire_early_alert` artık `_EARLY_FIRE_KEYS = ("dip",)` ile sınırlı:
- `approach` ve `m1_breakout` **kapıdan çıkarıldı** (OOS lift −0.082 / −0.094) → artık alarm ATEŞLEMEZ, tanımlayıcı kalır.
- `macd_dip_turn` tek yaşayan öncü, **PAPER-ONLY**: kanıt katmanı + WS'ye yazılır, web-push YOK (promotion kuralı).
- `_dip_detail` ısınma hatası `_MACD_MIN_CANDLES + 1` ile düzeltildi.
- Push bloğu `_ = settings` ile susturuldu; WS + evidence kaydı korundu.
- Testler güncellendi (yeni: approach/m1 ateşlemez, yalnız dip ateşler) → 77 test exit 0.
- `outputs/erken_oncu_replay_kanit.md` → §6 "UYGULANAN Aşama 3 kapısı" eklendi.
- C7 (adaptif cooldown) hâlâ replay-gated aday; C1-(sert)/C2/C3/C4/C5/C6/C8 Aktive edilmedi.

## Aşama 3 TAMAM + COMMIT/PUSH ✅
- **C7 adaptive cooldown implement edildi** (task #38/36 kapatıldı): `early_adaptive_cooldown` ayar anahtarı +
  `MACD_EARLY_ADAPTIVE_COOLDOWN` env (varsayılan false). `_early_adaptive_cooldown()` saf fonksiyonu dip için
  kanıt 30–150dk bandında ölçeklenir; kapalıyken sabit 30dk (davranış değişmez). `_early_last_gap` durumu.
- Testler: test_macd_monitor + alert_stats → **81 passed**; audit_logs+loop_wiring dahil → 95 passed. Tüm py_compile temiz.
- `config.py`, `macd_monitor.py`, `test_macd_monitor.py`, `outputs/erken_oncu_replay_kanit.md` (C7 bölümü) değişti.
- **Commit + push origin main başarılı.** Çalışma ağacı temiz.
- Task'lar: #25/26/36/37/38/39 completed. Madde 14/21 zaten 2026-09-10'da kapanmıştı (sade işaretleme).

## Aşama 4 — rejim & seans koşullu ölçüm (backend)
- Ölçüm katmanı (davranış değişmedi; gate hâlâ `_EARLY_FIRE_KEYS=("dip",)`):
  - `_regime_tag(row)`: SNAPSHOT alanlarından trend/dir + vol → etiket (trend/sideways · high/low).
  - `_session_tag(ts)`: Istanbul seans kovası (gece/sabah/oglen/ogleden_sonra/aksam/gece_gec).
  - `_record_alert_evidence` → `signals["regime"]` + `signals["session"]`.
  - `database.macd_monitor_alert_conditional_stats(days, dimension, min_n)` (grup n/avg/LİFT örnek korumalı).
  - API `GET /api/macd-monitor/conditional-stats`.
- Test: `backend/tests/test_macd_conditional_stats.py`. Rapor: `outputs/asama4_rejim_seans.md`.
- Commit+push tamam. **SINIRLAMA:** sandbox I/O kararsız — koşum görülemedi; frontend koşullu
  tablo eklenemedi. Sonraki adım: UI panel + tam suite koşumu.

## Aşama 4 TAMAMLANDI + COMMIT/PUSH ✅ (frontend panel + hata düzeltmesi)
- **`_baseline_summary` hata düzeltmesi:** önceki değişiklikte imza hatalıca `async def _baseline_summary()` olmuş ve `baselines` parametresi düşmüştü → `def _baseline_summary(baselines)` geri çevrildi (kırılmayı önler).
- `_regime_tag`/`_session_tag` yardımcıları + `_record_alert_evidence` → `signals["regime"]`/`["session"]` yeniden eklendi (önceki commit'te düşmüştü).
- `macd_monitor_alert_conditional_stats` artık düşük örnek grupları DAHİL eder, `low_sample: true` işaretler (UI gri gösterir).
- **Frontend paneli** (`frontend/app/macd-monitor/page.tsx`): "KOŞULLU İSABET (REJİM / SEANS)" — boyut seçici (rejim/seans), per-grup 5m/15m/30m n/ort/lift/isabet tablosu, `n < min_n` gruplar `opacity-45` + "AZ ÖRNEK" rozeti. `tsc --noEmit` temiz.
- Doğrulama: iç `venv` ile 389 backend testi pass (pool-stop uyarısı önceden mevcut, zararsız); py_compile temiz; typecheck exit 0.
- Rapor `outputs/asama4_regim_seans.md` güncellendi. Commit+push origin main yapıldı.