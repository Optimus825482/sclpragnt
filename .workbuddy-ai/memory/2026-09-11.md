# 2026-09-11 — Scalper Agent V4

## MACD MONITOR — Aşama 2 tamamlandı (task #34 ✅)
- `pre_detail` / `early_score` / `as_of` / per-(symbol,precursor) cooldown eklendi; `_early_trigger` kapısı.
- 76 test yeşil (test_macd_monitor.py + test_macd_alert_stats.py).
- Son kalan tek test fixture'ı düzeltildi: proximity fixture'ı prior_high'u seriden türetiyor.

## Aşama 3 ÖNKOŞULU — Erken öncü replay kanıtı (task #35 ✅)
Yeni betik `work/early_precursor_replay.py` (sızıntısız, 312 sembol, 13.487 kesit, ~65 gün). Sonuçlar:
- `outputs/erken_oncu_replay_kanit.md`.
- **Kritik bulgu:** `macd_dip_turn` üretimde ASLA 1 olamıyor (`_dip_detail` → `_macd`'a 34 bar, `_macd` ≥35 ister).
- `approach` ve `m1` bu evrende **ters (contrarian)**: OOS lift −0.08 / −0.09.
- `dip` (niyet) tek pozitif lift + en uzun lead (OOS +0.018).
- early_score sürekli değeri kesitsel IC **NEGATİF** (OOS −0.072, t −28): skor olarak UYGULANAMAZ, yalnız tanımlayıcı.
- Karar: C1(dip) düzeltilip tanımlayıcı; C2/C3/C5 REDDEDİLDİ; C4/C6/C8 veri yok; C7 adaptif cooldown gated.

## Evren/metrik not
- Evren 30m ortalama getiri ≈ +0.030%. Round-trip maliyet ≈ %0.35 (komisyon 0.15×2 + kayma 0.025×2).

## Aşama 3 KAPISI UYGULANDI (task #36, ilk parça ✅)
`_maybe_fire_early_alert` artık `_EARLY_FIRE_KEYS = ("dip",)` ile sınırlı:
- `approach` ve `m1_breakout` **kapıdan çıkarıldı** (OOS lift −0.082 / −0.094) → artık alarm ATEŞLEMEZ, tanımlayıcı kalır.
- `macd_dip_turn` tek yaşayan öncü, **PAPER-ONLY**: kanıt katmanı + WS'ye yazılır, web-push YOK (promotion kuralı).
- `_dip_detail` ısınma hatası `_MACD_MIN_CANDLES + 1` ile düzeltildi.
- Push bloğu `_ = settings` ile susturuldu; WS + evidence kaydı korundu.
- Testler güncellendi (yeni: approach/m1 ateşlemez, yalnız dip ateşler) → 77 test exit 0.
- `outputs/erken_oncu_replay_kanit.md` → §6 "UYGULANAN Aşama 3 kapısı" eklendi.
- C7 (adaptif cooldown) hâlâ replay-gated aday; C1-(sert)/C2/C3/C4/C5/C6/C8 Aktive edilmedi.