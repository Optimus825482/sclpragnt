# Optional local research dependencies. These are intentionally separate from
# the production runtime requirements. pandas is used by backend/scripts
# research tooling only, never by the app itself.
vectorbt==1.1.0
pandas==3.0.5
