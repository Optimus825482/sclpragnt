# Tüm servisler için Docker log rotasyonu (varsayılan sınırsız büyür).
# YAML anchor'ı kullanımdan ÖNCE tanımlanmalıdır.
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

# 2026-09-26 denetim düzeltmesi (#74): Sembol evreni ~309'a çıkmış durumda;
# backend tek başına çok sayıda sembol tarıyor. HİÇBİR serviste
# `mem_limit`/`cpus`/`deploy.resources` yoktu → tek host'ta bir konteynerin
# OOM'u tüm stack'i (postgres dahil) düşürüyordu, veri erişilemez hale
# geliyordu. Limitler sembol evreni büyüdükçe gözden geçirilmelidir.
# NOT: `deploy.resources.limits` Compose v2'de uygulanır ( swarm dışında da
# `mem_limit`/`cpus` bridge modunda desteklenir; ikisini birlikte yazmak
# Swarm uyumluluğunu korur).
services:
  postgres:
    image: pgvector/pgvector:pg17-bookworm
    restart: unless-stopped
    stop_grace_period: 10s
    # #74: pgvector indeksleri + 3 aylık trade geçmişi. 1G.
    mem_limit: 1g
    cpus: 1.0
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "1.0"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-scalper}
      POSTGRES_USER: ${POSTGRES_USER:-scalper}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      TZ: ${TZ:-Europe/Istanbul}
    volumes:
      - scalper_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-scalper} -d ${POSTGRES_DB:-scalper}"]
      start_period: 30s
      interval: 10s
      timeout: 5s
      retries: 12
    logging: *default-logging
  backend:
    build:
      context: ./backend
    restart: unless-stopped
    stop_grace_period: 10s
    # #74: en yüksek tüketici — 309 sembollük evrende scanner + LLM + WS
    # yayıncısı aynı süreçte. 2G / 2 CPU.
    mem_limit: 2g
    cpus: 2.0
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2.0"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # 2026-09-26 (#74/a): PRODUCTION_MODE.
      #
      # `config.py` PRODUCTION_MODE'u `SCALPER_ENV > ENVIRONMENT >
      # "development"` sırasıyla okur. Varsayılan "development" iken
      # `enforce_admin_password_policy(..., production=False)` zayıf admin
      # parolasını yalnızca UYARI düzeyine indirir.
      #
      # ⚠️ GEÇİCİ GERİ ALMA (2026-09-26, operatör kararı — Seçenek B):
      # "production" DEĞERİ DENENDİ ve DEPLOY'U DURDURDU. Mevcut
      # SCALPER_ADMIN_PASSWORD politikayı geçmiyor (<10 karakter / yaygın
      # sözlük), `config.py` import sırasında RuntimeError fırlatıyor,
      # uvicorn hiç açılmıyor, healthcheck geçmiyor, konteyner "Waiting"
      # durumunda kalıyor.
      #
      # Bu değer "production" yapıldığında konteyner açılmayı REDDEDER —
      # yani politika gerçekten uygulanır, ama mevcut parolayla prod'a
      # çıkmak mümkün değildir. Parolayı güçlendirip (>=10 karakter, harf+
      # rakam, yaygın sözlük değil) bu değeri "production" yapmak gerekir.
      # Politika: `config.py:admin_password_policy_message`.
      SCALPER_ENV: ${SCALPER_ENV:-development}
      SCALPER_DB_PATH: /data/scalper_db_v4.sqlite
      # Use the database service in this Compose stack. A stale Coolify
      # DATABASE_URL must not redirect the backend to an unreachable host.
      DATABASE_URL: postgresql://${POSTGRES_USER:-scalper}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}@postgres:5432/${POSTGRES_DB:-scalper}
      # This application is PostgreSQL-only. Do not allow a stale Coolify
      # variable to silently switch the backend to the retired SQLite path.
      DB_BACKEND: postgres
      LIVE_TRADING: "false"
      USE_TESTNET: "false"
      # TR saati: quiet-hours ve "gün" sınırları yerel zaman okuyan kod
      # path'leriyle tutarlı olsun (konteyner varsayılanı UTC'dir).
      TZ: ${TZ:-Europe/Istanbul}
      CORS_ORIGINS: https://scalper.erkanerdem.online
      SCALPER_ADMIN_PASSWORD: ${SCALPER_ADMIN_PASSWORD:?SCALPER_ADMIN_PASSWORD must be set}
      SCALPER_SESSION_SECRET: ${SCALPER_SESSION_SECRET:?SCALPER_SESSION_SECRET must be set}
      SCALPER_COOKIE_SECURE: "1"
      LLM_ENCRYPTION_KEY: ${LLM_ENCRYPTION_KEY:?LLM_ENCRYPTION_KEY must be set}
      LLM_ALLOW_PRIVATE_PROVIDER: ${LLM_ALLOW_PRIVATE_PROVIDER:-0}
      TOP_GAINERS_AUTO_ACTIVATE: ${TOP_GAINERS_AUTO_ACTIVATE:-true}
      TOP_GAINERS_LIMIT: ${TOP_GAINERS_LIMIT:-10}
      TOP_GAINERS_REFRESH_SEC: ${TOP_GAINERS_REFRESH_SEC:-600}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY:-}
      VAPID_SUBJECT: ${VAPID_SUBJECT:-mailto:alerts@example.com}
    # 2026-09-26 (#75): Aşağıdaki secret'lar plaintext `environment:` ile
    # veriliyor: POSTGRES_PASSWORD, SCALPER_ADMIN_PASSWORD,
    # SCALPER_SESSION_SECRET, LLM_ENCRYPTION_KEY. `docker inspect` /
    # `docker compose config` bunları düz metin gösterir ve CI log'larına
    # sızabilir.
    #
    # ÖNERİLEN GEÇİŞ — Coolify'ta secret mount ile (bilinçli olarak UYGULANMADI;
    # prod kırılmasın diye dokunulmadı):
    #
    #   Coolify → Resource → "Secret Files" bölümünden
    #     /run/secrets/llm_encryption_key  (64 hex, 32 bayt)
    #     /run/secrets/scalper_admin_password
    #     /run/secrets/scalper_session_secret
    #   dosyaları mount edilir, ardından yukarıdaki satırlar şu hale gelir:
    #
    #     secrets:
    #       - llm_encryption_key
    #     ...
    #     LLM_ENCRYPTION_KEY_FILE: /run/secrets/llm_encryption_key
    #     SCALPER_ADMIN_PASSWORD_FILE: /run/secrets/scalper_admin_password
    #     SCALPER_SESSION_SECRET_FILE: /run/secrets/scalper_session_secret
    #
    #   ...ve dosya-istemcisinin `os.getenv("X") or open(os.getenv("X_FILE"))`
    #   deseniyle okuması gerekir. BU, `backend/app/config.py` ve
    #   `docker-compose.yaml` dışında değişiklik gerektirir (config.py başka
    #   ajanların sahipliğinde) — birlikte planlanmalıdır.
    #
    # Aynı desen postgres için de geçerlidir (`POSTGRES_PASSWORD_FILE` resmi
    # postgres imajı tarafından desteklenir; backend tarafı yine config.py
    # değişikliği gerektirir).
    volumes:
      - scalper_data:/data
    expose:
      - "8004"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8004/health', timeout=3)"]
      # Initial history hydration for the full dynamic symbol universe can
      # exceed the first few probes. Retain fast promotion once ready, while
      # avoiding an early Compose abort that leaves dependents as "Created".
      start_period: 90s
      interval: 15s
      timeout: 5s
      retries: 20
    logging: *default-logging

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_VAPID_PUBLIC_KEY: ${NEXT_PUBLIC_VAPID_PUBLIC_KEY:-}
    restart: unless-stopped
    stop_grace_period: 5s
    # #74: Next.js build sürecinde (node) ve çalışırken 1G / 1 CPU.
    mem_limit: 1g
    cpus: 1.0
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "1.0"
    depends_on:
      backend:
        # The UI shell does not require the API to be healthy at process
        # start. Avoid leaving it in Created when backend warmup is slow.
        condition: service_started
    environment:
      NEXT_PUBLIC_VAPID_PUBLIC_KEY: ${NEXT_PUBLIC_VAPID_PUBLIC_KEY:-}
    expose:
      - "3004"
    healthcheck:
      # Next 0.0.0.0'a bağlı (HOSTNAME=0.0.0.0 Dockerfile'da); 127.0.0.1'e
      # bağlanır. localhost yerine 127.0.0.1 — IPv6 loopback trap'ini önler.
      test: ["CMD-SHELL", "wget --spider -q http://127.0.0.1:3004/ || exit 1"]
      start_period: 20s
      interval: 30s
      timeout: 5s
      retries: 3
    logging: *default-logging

  gateway:
    build:
      context: ./nginx
    image: scalperagent-gateway:latest
    restart: unless-stopped
    stop_grace_period: 5s
    # #74: nginx düşük ayak izli; 1G üst sınır yeterli (rate-limit zone'ları
    # 10m × 3 = ~30 MB). 1 CPU.
    mem_limit: 1g
    cpus: 1.0
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: "1.0"
    depends_on:
      - frontend
      - backend
    expose:
      - "80"
    # Traefik (coolify-proxy) yalnızca kendi ağında bulduğu konteynerlere
    # route oluşturur. Gateway bu ağa bağlanmazsa domain router'ı hiç
    # üretilmez → dışarıdan "no available server". External network adı
    # Coolify kurulumunda varsayılan olarak 'coolify'dır.
    networks:
      # coolify ağı EXTERNAL: yerelde (`docker compose up` ile) çalıştırmak
      # İMKANSIZDIR — ağ compose dışında tanımlı olmalıdır:
      #   docker network create coolify
      # Coolify kurulumunda ağ zaten vardır. Bilerek değiştirilmedi.
      - default
      - coolify
    healthcheck:
      # nginx'in kendi canlılığı: upstream'lere bağımlı değil. /health
      # (backend proxy'si) kullanılsaydı backend'deki her kısa tıkanıklık
      # gateway'i unhealthy yapar, Coolify/Traefik de devreden çıkarırdı.
      # NOT: `localhost` yerine 127.0.0.1 — /etc/hosts'ta localhost ::1'e
      # çözümlenir; nginx yalnızca IPv4 (0.0.0.0:80) dinlediğinden wget
      # localhost'a gidince "Connection refused" alıyordu → unhealthy → 503.
      test: ["CMD-SHELL", "wget --spider -q http://127.0.0.1:80/gateway-health || exit 1"]
      start_period: 10s
      interval: 30s
      timeout: 5s
      retries: 3
    logging: *default-logging

  # Otomatik gece yedeği: her 24 saatte bir pg_dump. Saklama 1 GÜN (2026-09-16):
  # 7 günlük pencere DB boyutunun 7 KATI yer tutuyordu (tek disk, %86 dolu) ve
  # en güncel dump eski bir dump'tan daha değerlidir. Deploy öncesi elle yedek
  # almak (aşağıdaki restore komutu) hâlâ önerilir.
  # ./local_backups host'a bağlanır (gitignored).
  # Restore: `cat <dump> | docker exec -i <postgres> psql -U scalper scalper`
  db-backup:
    image: pgvector/pgvector:pg17-bookworm
    restart: unless-stopped
    # #74: pg_dump büyük tablolarda bellek şişebilir; 512M üst sınır yeterli
    # ve bu konteynerin host'u tüketmesini engeller.
    mem_limit: 512m
    cpus: 0.5
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.5"
    depends_on:
      backend:
        # Backend ancak migration'ı geçince sağlıklı olur. pg_dump tüm
        # tablolara dakikalarca ACCESS SHARE kilit alır; migration'ın
        # ALTER TABLE'ı ile açılış yarışını böylece kökten engelleriz.
        condition: service_healthy
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
    volumes:
      - ./local_backups:/backups
    entrypoint:
      - /bin/sh
      - -c
      - |
        # Ilk dump oncesi ek guvenlik penceresi (deploy/migration tamamen
        # dursun diye); sonraki donguler 86400s uyur, yarisma olmaz.
        sleep 300
        # 2026-09-16 disk denetimi: `-mtime +1` YETMEDI. Iki neden:
        #   1) `-mtime +1` "2 gunden eski" demektir → 2 gune kadar her sey kalir.
        #   2) Temizlik yalnizca dump'tan SONRA, dongude bir kez kosuyordu; her
        #      DEPLOY yeni konteyner actigi icin gunde birden fazla dump birikti
        #      (14 Eylul'de 5 dump). Olcum: local_backups = 69 GB.
        # Cozum: SAYI tabanli saklama — en yeni 2 dump kalir, yastan bagimsiz.
        # Ayrica dongunun BASINDA da temizlenir ki redeploy firtinasinda birikmesin.
        # Boyut sinirli kalir: ~2 x dump (bloat sonrasi ~725 MB -> ~1.5 GB).
        while true; do
          ts=$$(date +%Y%m%d_%H%M%S)
          ls -1t /backups/scalper_*.dump 2>/dev/null | tail -n +3 | xargs -r rm -f
          echo "[backup] $$ts basliyor"
          if pg_dump -h postgres -U ${POSTGRES_USER:-scalper} -d ${POSTGRES_DB:-scalper} -Fc -f "/backups/scalper_$$ts.dump"; then
            echo "[backup] $$ts tamam"
          else
            echo "[backup] $$ts BASARISIZ" >&2
          fi
          ls -1t /backups/scalper_*.dump 2>/dev/null | tail -n +3 | xargs -r rm -f
          sleep 86400
        done
    logging: *default-logging

volumes:
  scalper_data:
  scalper_postgres_data:

# Coolify proxy (Traefik) ağı — compose dışında tanımlı external ağ.
networks:
  coolify:
    external: true
    name: coolify
